[tool.poetry]
name = "poetry"
version = "0.1.0"
description = ""
authors = ["marcopra <marco.prattico@live.it>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
